/**
 * Defined constants for mailbox pages (values also double as translation keywords).
 */
export class FolderTypes {
  static readonly INBOX: string = "INBOX_FOLDER";
  static readonly SENT: string = "SENT_FOLDER";
}
