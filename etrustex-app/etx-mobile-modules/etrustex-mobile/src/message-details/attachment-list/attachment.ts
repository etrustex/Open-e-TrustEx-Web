/**
 * Attachment class holds information about one attachment.
 * Attachments are only displayed as a list without any
 * user interaction.
 */
export class Attachment {
  id: string;
  messageId: string;
  name: string;
  mimeType: string;
}
